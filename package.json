{
  "name": "extract-gsheet",
  "version": "0.1.0",
  "description": "A semi-reliable half-hacky way to extract data from a Google Sheet.",
  "main": "index.js",
  "scripts": {
    "dev": "npm run clean:dev && cross-env ELEVENTY_ENV=dev npx @11ty/eleventy --serve --quiet",
    "build": "npm run clean && cross-env ELEVENTY_ENV=prod npx @11ty/eleventy",
    "clean:dev": "npx rimraf ./dev",
    "clean": "npx rimraf ./docs ./dev",
    "rollup": "rollup -c",
    "rollup:watch": "rollup -c -w"
  },
  "keywords": [
    "Google",
    "Sheets",
    "Spreadsheets",
    "Prototype"
  ],
  "author": "Nathan Blaylock",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "cross-env": "^7.0.3",
    "eleventy-plugin-sass": "^1.2.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.59.0",
    "rollup-plugin-terser": "^7.0.2"
  },
  "dependencies": {
    "bootstrap": "^5.1.3"
  }
}
